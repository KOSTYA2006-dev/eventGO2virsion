# Быстрый старт EventGo

## Минимальная настройка для запуска

1. **Установите зависимости:**
```bash
composer install
```

2. **Создайте `.env` файл:**
```bash
cp .env.example .env
php artisan key:generate
```

3. **Настройте базу данных в `.env`:**
```env
DB_CONNECTION=sqlite
```
Или используйте MySQL/PostgreSQL с настройками:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=eventgo
DB_USERNAME=root
DB_PASSWORD=
```

4. **Создайте базу данных (для SQLite):**
```bash
touch database/database.sqlite
```

5. **Выполните миграции и заполните тестовыми данными:**
```bash
php artisan migrate
php artisan db:seed
```

6. **Запустите сервер:**
```bash
php artisan serve
```

7. **Откройте в браузере:**
- Главная страница: http://localhost:8000
- Админ-панель: http://localhost:8000/admin/login
  - Email: `admin@eventgo.ru`
  - Пароль: `admin123`

## Настройка почты (для отправки чеков)

Для тестирования используйте `log` драйвер (письма будут сохраняться в `storage/logs/laravel.log`):

```env
MAIL_MAILER=log
MAIL_FROM_ADDRESS=noreply@eventgo.ru
MAIL_FROM_NAME="EventGo"
```

Для продакшена настройте SMTP:
```env
MAIL_MAILER=smtp
MAIL_HOST=smtp.your-provider.com
MAIL_PORT=587
MAIL_USERNAME=your_username
MAIL_PASSWORD=your_password
MAIL_FROM_ADDRESS=noreply@eventgo.ru
MAIL_FROM_NAME="EventGo"
```

## Настройка платежных реквизитов

В `.env` укажите ваши реквизиты ВТБ:
```env
VTB_ACCOUNT=ваш_расчетный_счет
VTB_BIK=044525187
VTB_INN=ваш_инн
VTB_KPP=ваш_кпп
RECIPIENT_NAME="Название организации"
```

## Настройка даты мероприятия

В `.env` укажите дату и время мероприятия:
```env
EVENT_DATE=2025-12-31 18:00:00
```

## Тестовые данные

После выполнения `php artisan db:seed` будут созданы:

**Билеты:**
- Обычный билет - 5000 ₽
- VIP билет - 15000 ₽

**Промокоды:**
- `WELCOME10` - скидка 10%
- `VIP500` - скидка 500 ₽
- `EARLY20` - скидка 20%

**Администратор:**
- Email: `admin@eventgo.ru`
- Пароль: `admin123`

## Проверка работы

1. Откройте главную страницу
2. Выберите билет
3. Заполните форму заказа
4. Примените промокод (опционально)
5. Выберите способ оплаты (QR или СБП)
6. Нажмите "Я оплатил(а) заказ"
7. Проверьте email (или логи) - должен прийти чек

## Админ-панель

В админ-панели вы можете:
- Просматривать все заказы
- Видеть информацию о покупателях (фамилия, имя, телефон, email, вид деятельности)
- Управлять билетами
- Управлять промокодами
- Видеть статистику

## Решение проблем

**Ошибка подключения к БД:**
- Проверьте настройки в `.env`
- Убедитесь, что база данных создана
- Для SQLite убедитесь, что файл `database/database.sqlite` существует

**Письма не отправляются:**
- Проверьте настройки почты в `.env`
- Для тестирования используйте `MAIL_MAILER=log`
- Проверьте логи в `storage/logs/laravel.log`

**QR код не отображается:**
- Проверьте интернет-соединение (используется внешний API)
- Проверьте настройки платежных реквизитов

**Ошибки миграций:**
- Убедитесь, что база данных пустая или выполните `php artisan migrate:fresh`

---

Готово! Ваш сайт готов к работе! 🎉

