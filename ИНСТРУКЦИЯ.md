# Инструкция по настройке автоматической оплаты

## ✅ Что было сделано:

1. **Интегрирован платежный сервис YooKassa**
   - Оплата теперь происходит через готовый сервис YooKassa
   - Пользователь перенаправляется на страницу оплаты YooKassa

2. **Настроена автоматическая обработка оплаты**
   - Webhook автоматически обрабатывает уведомления от YooKassa
   - Статус заказа обновляется автоматически после оплаты

3. **Автоматическая отправка чека и билета на email**
   - После успешной оплаты автоматически генерируется чек
   - Автоматически генерируется билет
   - Оба файла автоматически отправляются на email покупателя

4. **Установлена цена всех билетов на 10 рублей**
   - Все билеты теперь стоят 10 рублей для тестирования

## 🔧 Настройка:

### 1. Настройте YooKassa:

1. Зарегистрируйтесь на https://yookassa.ru/
2. Получите Shop ID и Secret Key в разделе "Настройки" → "API"
3. Добавьте в `.env`:
   ```env
   YOOKASSA_SHOP_ID=ваш_shop_id
   YOOKASSA_SECRET_KEY=ваш_секретный_ключ
   YOOKASSA_TEST_MODE=true
   USE_YOOKASSA=true
   ```

### 2. Настройте webhook:

1. В личном кабинете YooKassa: https://yookassa.ru/my
2. Раздел "Настройки" → "Уведомления"
3. Добавьте URL: `https://ваш-домен.ru/payment/webhook`
4. Выберите события: `payment.succeeded` и `payment.canceled`

### 3. Настройте email:

Добавьте в `.env`:
```env
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=ваш_email@gmail.com
MAIL_PASSWORD=пароль_приложения
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=ваш_email@gmail.com
MAIL_FROM_NAME="EventGo"
```

## 🚀 Как это работает:

1. Пользователь создает заказ → система создает платеж в YooKassa
2. Пользователь оплачивает на странице YooKassa
3. YooKassa отправляет webhook на ваш сервер
4. **Автоматически:**
   - Обновляется статус заказа
   - Генерируется чек
   - Генерируется билет
   - Чек и билет отправляются на email покупателя

## 🧪 Тестирование:

- **Тестовая карта:** `5555555555554444` (любая дата, любой CVV)
- Все билеты стоят **10 рублей** для тестирования
- После оплаты автоматически придет чек и билет на email

## 📝 Команды:

```bash
# Установить цену билетов на 10 рублей
php artisan tickets:set-price 10

# Выполнить миграции (если нужно)
php artisan migrate
```

## ⚠️ Важно:

- Убедитесь, что webhook URL доступен из интернета
- Проверьте настройки email перед продакшеном
- В тестовом режиме YooKassa не списывает реальные деньги
- Все логи сохраняются в `storage/logs/laravel.log`

## 📧 Проверка работы:

После оплаты проверьте:
1. Email покупателя (чек и билет должны прийти автоматически)
2. Статус заказа в админ-панели (должен быть "оплачен")
3. Логи в `storage/logs/laravel.log`

---

**Всё готово к работе!** 🎉

