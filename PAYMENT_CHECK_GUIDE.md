# Руководство по автоматической проверке оплаты

## 🎯 Способы проверки оплаты

### 1. Тестовая проверка для заказов на 10 рублей

#### Через админ-панель:
1. Зайдите в админ-панель → Заказы
2. Найдите заказ на сумму **10 рублей** со статусом "Ожидает оплаты"
3. Нажмите кнопку **"🧪 Тестовая проверка (10₽)"**
4. Чек автоматически отправится на email покупателя

#### Через API:
```bash
POST /payment/test-check/{order_id}
```

#### Через команду Artisan:
```bash
php artisan payments:check --order={order_id} --test
```

### 2. Ручная проверка любого заказа

#### Через админ-панель:
1. Зайдите в админ-панель → Заказы
2. Найдите заказ со статусом "Ожидает оплаты"
3. Нажмите кнопку **"Подтвердить оплату"**
4. Чек автоматически отправится на email покупателя

### 3. Автоматическая проверка через команду

#### Проверка всех неоплаченных заказов:
```bash
php artisan payments:check
```

#### Проверка только заказов на 10 рублей:
```bash
php artisan payments:check --test
```

#### Проверка конкретного заказа:
```bash
php artisan payments:check --order=1
```

#### Проверка конкретного заказа на 10 рублей:
```bash
php artisan payments:check --order=1 --test
```

### 4. Webhook для автоматической проверки от банка

#### URL webhook:
```
POST /payment/webhook
```

#### Формат запроса:
```json
{
  "order_number": "ORD-XXXXXXXX",
  "amount": 10.00,
  "status": "paid"
}
```

#### Пример тестирования через curl:
```bash
curl -X POST http://your-domain.com/payment/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "order_number": "ORD-XXXXXXXX",
    "amount": 10.00,
    "status": "paid"
  }'
```

#### Настройка в банке:
1. Зайдите в настройки интернет-банка или кабинета ВТБ
2. Найдите раздел "Webhook" или "Уведомления"
3. Укажите URL: `https://your-domain.com/payment/webhook`
4. Банк будет автоматически отправлять уведомления об оплате

## 🔄 Автоматический запуск проверки

### Настройка cron для автоматической проверки

Добавьте в crontab (Linux/Mac):
```bash
* * * * * cd /path-to-your-project && php artisan schedule:run >> /dev/null 2>&1
```

Или настройте в `routes/console.php`:
```php
use Illuminate\Support\Facades\Schedule;

Schedule::command('payments:check --test')
    ->everyFiveMinutes()
    ->withoutOverlapping();
```

## 📧 Что происходит при проверке оплаты

1. ✅ Статус заказа меняется на "Оплачен"
2. 📄 Генерируется чек с банковскими реквизитами
3. 📧 Чек отправляется на email покупателя
4. 💾 Чек сохраняется в системе (можно скачать в админке)

## 🧪 Тестирование на 10 рублей

Для тестирования системы:

1. **Создайте тестовый билет на 10 рублей:**
   - Зайдите в админ-панель → Билеты
   - Создайте билет с ценой 10 рублей

2. **Создайте заказ:**
   - Перейдите на главную страницу
   - Выберите билет на 10 рублей
   - Заполните форму заказа
   - Выберите способ оплаты

3. **Проверьте оплату:**
   - В админ-панели найдите созданный заказ
   - Нажмите "🧪 Тестовая проверка (10₽)"
   - Проверьте email покупателя - должен прийти чек

## ⚠️ Важные замечания

- Тестовая проверка работает **только для заказов на сумму 10 рублей**
- Для других сумм используйте обычную проверку оплаты
- Webhook требует настройки в банке
- Все действия логируются в `storage/logs/laravel.log`

## 🔍 Проверка логов

Для просмотра логов проверки оплаты:
```bash
tail -f storage/logs/laravel.log | grep -i payment
```

Или используйте команду:
```bash
php artisan pail
```

## 📞 Поддержка

При возникновении проблем проверьте:
1. Настройки email в `.env`
2. Логи в `storage/logs/laravel.log`
3. Статус заказа в админ-панели
4. Настройки webhook в банке

