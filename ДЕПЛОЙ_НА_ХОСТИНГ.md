# 🚀 Быстрый деплой на хостинг

## Подготовка к деплою

### 1. Выберите хостинг

Рекомендуемые хостинги для Laravel:
- **Timeweb** - российский хостинг, хорошая поддержка PHP 8.1+
- **Beget** - популярный российский хостинг
- **REG.RU** - надежный хостинг с поддержкой Laravel
- **VPS** (DigitalOcean, Vultr, Hetzner) - для полного контроля

### 2. Требования хостинга

- PHP 8.1 или выше
- MySQL 5.7+ или MariaDB 10.3+
- Composer
- Расширения PHP: OpenSSL, PDO, Mbstring, Tokenizer, XML, Ctype, JSON, BCMath
- Минимум 512MB RAM
- SSL сертификат (для YooKassa обязательно!)

## Пошаговая инструкция

### Шаг 1: Подготовка проекта

```bash
# 1. Убедитесь, что все зависимости установлены
composer install --optimize-autoloader --no-dev
:cache
php artisan route:cache
php artisan view:cache
```

### Шаг 2: Загрузка файлов на хостинг

**Вариант A: Через FTP/SFTP**
1. Подключитесь к хостингу через FileZilla или WinSCP
2. Загрузите все файлы проекта в папку `public_html` или `www`
3. **ВАЖНО:** Убедитесь, что файлы из папки `public` находятся в корне `public_html`

**Вариант B: Через Git (рекомендуется)**
```bash
# На хостинге
git clone https://github.com/ваш-репозиторий/eventgo.git
cd eventgo
composer install --optimize-autoloader --no-dev
```

### Шаг 3: Настройка базы данных

1. Создайте базу данных в панели хостинга
2. Запишите данные:
   - Имя базы данных
   - Пользователь
   - Пароль
   - Хост (обычно `localhost`)

3. Обновите `.env`:
```env
DB_CONNECTION=mysql
DB_HOST=localhost
# 2. Создайте .env файл для продакшена
cp .env.example .env

# 3. Сгенерируйте ключ приложения
php artisan key:generate

# 4. Оптимизируйте приложение
php artisan config
DB_PORT=3306
DB_DATABASE=ваша_база_данных
DB_USERNAME=ваш_пользователь
DB_PASSWORD=ваш_пароль
```

4. Выполните миграции:
```bash
php artisan migrate
```

### Шаг 4: Настройка .env для продакшена

```env
APP_NAME="EventGo"
APP_ENV=production
APP_KEY=base64:ваш_ключ_из_artisan_key_generate
APP_DEBUG=false
APP_URL=https://ваш-домен.ru

# База данных
DB_CONNECTION=mysql
DB_HOST=localhost
DB_DATABASE=ваша_база
DB_USERNAME=ваш_пользователь
DB_PASSWORD=ваш_пароль

# YooKassa
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_секретный_ключ
YOOKASSA_TEST_MODE=false
USE_YOOKASSA=true

# Email (SMTP)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=ваш_email@gmail.com
MAIL_PASSWORD=пароль_приложения
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=ваш_email@gmail.com
MAIL_FROM_NAME="EventGo"

# Контакты
CONTACT_EMAIL=info@eventgo.ru
CONTACT_PHONE=+7 (999) 123-45-67

# Реквизиты
RECIPIENT_NAME=ИП ЛАЗАРЕВА СВЕТЛАНА ИГОРЕВНА
VTB_ACCOUNT=40802810506640007313
VTB_BIK=044525411
VTB_INN=616404172802
VTB_CORRESPONDENT_ACCOUNT=30101810145250000411
BANK_NAME=ФИЛИАЛ "ЦЕНТРАЛЬНЫЙ" БАНКА ВТБ (ПАО)
OGRNIP=316616400101234
```

### Шаг 5: Настройка структуры папок

**Для большинства хостингов:**
```
public_html/
├── index.php (из public/index.php)
├── .htaccess (из public/.htaccess)
├── assets/
└── ... (остальные файлы из public/)

Корень проекта:
├── app/
├── bootstrap/
├── config/
├── database/
├── resources/
├── routes/
├── storage/
├── vendor/
└── .env
```

**Или настройте через .htaccess:**
```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
```

### Шаг 6: Настройка прав доступа

```bash
# На хостинге через SSH
chmod -R 755 storage bootstrap/cache
chmod -R 755 public
```

### Шаг 7: Настройка YooKassa Webhook

1. Войдите в личный кабинет YooKassa: https://yookassa.ru/my
2. Перейдите в "Настройки" → "Уведомления"
3. Добавьте URL: `https://ваш-домен.ru/payment/webhook`
4. Выберите события:
   - ✅ `payment.succeeded`
   - ✅ `payment.canceled`

### Шаг 8: Создание билетов и тестовые данные

```bash
# Обновить информацию о билетах
php artisan tickets:update-info

# Установить цену на 10 рублей (для тестирования)
php artisan tickets:set-price 10
```

### Шаг 9: Проверка работы

1. Откройте сайт: `https://ваш-домен.ru`
2. Проверьте страницы:
   - Главная: `https://ваш-домен.ru`
   - Реквизиты: `https://ваш-домен.ru/requisites`
   - Оферта: `https://ваш-домен.ru/agreement`
   - Доставка: `https://ваш-домен.ru/delivery`
   - Контакты: `https://ваш-домен.ru/contacts`

3. Создайте тестовый заказ и проверьте оплату

## Быстрый деплой через Timeweb (пример)

### 1. Подключение через SSH

```bash
ssh user@ваш_сервер
cd ~/public_html
```

### 2. Загрузка файлов

```bash
# Через Git
git clone https://github.com/ваш-репозиторий/eventgo.git .
composer install --optimize-autoloader --no-dev

# Или через FTP загрузите файлы
```

### 3. Настройка

```bash
cp .env.example .env
nano .env  # Отредактируйте настройки
php artisan key:generate
php artisan migrate
php artisan config:cache
```

### 4. Настройка домена

В панели Timeweb:
- Привяжите домен к папке `public_html`
- Включите SSL сертификат (Let's Encrypt - бесплатно)

## Проверка после деплоя

✅ Сайт открывается  
✅ Страницы с реквизитами доступны  
✅ YooKassa webhook настроен  
✅ Email отправляется  
✅ База данных работает  
✅ SSL сертификат активен  

## Полезные команды

```bash
# Очистка кэша
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

# Оптимизация
php artisan config:cache
php artisan route:cache
php artisan view:cache

# Проверка логов
tail -f storage/logs/laravel.log
```

## Решение проблем

### Ошибка 500
- Проверьте права доступа: `chmod -R 755 storage bootstrap/cache`
- Проверьте `.env` файл
- Проверьте логи: `storage/logs/laravel.log`

### YooKassa не работает
- Убедитесь, что SSL сертификат активен
- Проверьте webhook URL в настройках YooKassa
- Проверьте логи webhook запросов

### Email не отправляется
- Проверьте настройки SMTP в `.env`
- Для Gmail используйте пароль приложения
- Проверьте логи: `storage/logs/laravel.log`

## Безопасность

1. ✅ Установите `APP_DEBUG=false` в продакшене
2. ✅ Используйте сильные пароли для базы данных
3. ✅ Включите SSL сертификат
4. ✅ Регулярно обновляйте зависимости: `composer update`
5. ✅ Делайте резервные копии базы данных

---

**Готово! Ваш сайт должен работать на хостинге!** 🎉

